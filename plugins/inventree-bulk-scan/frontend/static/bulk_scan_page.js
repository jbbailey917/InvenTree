/* Bulk Scan Page — stub frontend */

(function() {
    const root = document.getElementById('bulk-scan-root');
    if (root) {
        root.innerHTML = '<h2>Bulk Scan</h2><p>Scan interface loading...</p>';
    }
})();
